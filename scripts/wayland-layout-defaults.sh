#!/usr/bin/env bash
# Known-good Wayland layout defaults for GrayHairedDesktop.
#
# Keep these values centralized so installation, verification, and future
# controlled layout experiments agree on the same baseline. Changing these
# values changes the generated user-local DING/WebKit child on the next
# install/reinstall; it does not alter an already-running desktop by itself.

GRAYHAIRED_WAYLAND_ICON_STRIP_WIDTH=220
GRAYHAIRED_WAYLAND_DESKTOP_URL="https://grayhaired.tech/desktop-d"
